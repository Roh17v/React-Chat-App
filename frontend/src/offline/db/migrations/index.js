// SQL migration files (`<n>__<name>.sql`) live in this directory and are
// registered in `../Migrator.js` as `MIGRATIONS = [m1, m2, ...]`.
// First migration `001__init.sql` lands in task 6.1.
export {};
